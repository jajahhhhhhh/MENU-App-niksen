node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# SQLite database — runtime data, never commit (may contain customer info)
*.db
*.db-journal
*.db-wal
*.db-shm

# playwright-cli session scratch (snapshots, screenshots)
.playwright-cli/

# Uploaded menu photos — runtime data, written next to pos.db
photos/
